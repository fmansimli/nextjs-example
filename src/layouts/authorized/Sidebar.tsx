import { FC } from "react";

interface IProps {}

const Sidebar: FC<IProps> = (props) => {
  return (
    <aside className="flex min-h-screen items-center justify-center">
      <div></div>
    </aside>
  );
};

export default Sidebar;
